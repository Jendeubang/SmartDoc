# 第一章 绪论

## 1.1 项目背景与意义

近年来，以 ChatGPT 为代表的大语言模型（Large Language Model, LLM）技术取得了突破性进展，深刻改变了人机交互方式与信息处理范式。从 GPT-3 的涌现能力到 GPT-4 的多模态理解，LLM 在自然语言理解、文本生成和复杂推理等任务上展现了前所未有的能力。然而，通用大模型在企业级文档处理场景中仍面临严峻挑战：模型训练数据存在知识截止日期，无法覆盖企业内部私有文档；模型在缺少领域上下文的情况下容易产生"幻觉"（hallucination），生成不准确甚至错误的信息；此外，企业文档管理涉及多用户协作、版本控制、权限管理等一系列复杂业务需求，单一的大模型对话接口远不能满足实际生产需要。

在数字化转型浪潮下，企业对智能文档处理平台的需求日益迫切。传统的文档管理系统（如 SharePoint、Confluence 等）虽然提供了基础的文档存储、版本管理和协作编辑能力，但缺乏 AI 驱动的智能分析、自动摘要、知识问答等高级功能。而飞书文档、钉钉文档等现代协作平台虽然集成了部分 AI 能力，但其 AI 功能多以通用问答或模板化工具为主，缺乏对用户私有文档进行深度知识挖掘和自主任务规划的能力。

本文设计的 DocAI 系统正是在此背景下应运而生。该系统以一个基于微服务架构的智能文档协作平台为载体，深度融合了检索增强生成（Retrieval-Augmented Generation, RAG）和 AI Agent（自主规划-执行）两大核心技术，旨在解决以下核心痛点：（1）私有知识库问答——通过 RAG 技术将企业内部文档进行分段、向量化索引和混合检索，使大模型能够基于文档内容生成有理有据的回答；（2）自主任务规划与执行——利用 AI Agent 的 ReAct 模式，使系统能够根据用户自然语言指令自动拆解任务、编排工具链并逐步执行；（3）实时协作与文档管理——通过 WebSocket 实时通信和操作转换（OT）冲突处理机制支持多人同时编辑；（4）全链路可观测运维——实现 AIOps 自动埋点和滑动窗口指标统计。

本项目通过 Docker Compose 容器化编排，将 5 个微服务、4 个中间件和前端应用整合为一键部署方案，为同类系统的工业化落地提供了可复用的参考架构。

## 1.2 国内外研究现状

### 1.2.1 RAG 技术发展

检索增强生成（RAG）的概念最早由 Lewis 等人在 2020 年的 NeurIPS 会议上系统提出，其核心思想是将预训练的序列到序列模型与外部非参数化记忆（即稠密向量索引）相结合，在生成答案时先从知识库中检索相关文档片段，再基于检索结果生成回答。该论文在三个开放域问答任务上取得了当时最优的结果，并证明了 RAG 模型比纯参数化模型能生成更具体、更多样化和更准确的语言。

在此基础之上，RAG 技术沿着多个方向快速演进。Karpukhin 等人提出的 Dense Passage Retrieval（DPR）使用双编码器架构将问题和文档映射到同一向量空间，显著提升了稠密检索的精度。Guu 等人提出的 REALM 通过掩码语言模型预训练任务端到端地学习检索和生成。Borgeaud 等人提出的 RETRO 模型在训练过程中引入大规模检索，证明了检索增强不仅可以用于推理阶段，还能有效改善模型的语言建模能力。Izacard 等人的 Atlas 模型则进一步探索了少样本学习场景下的检索增强范式。

在检索质量提升方面，研究者们提出了多种分段策略和重排序方法。常见的分段策略包括按固定长度切分、按章节结构切分、基于语义相似度的自适应切分以及多种策略混合的分段方案。重排序方面，BM25 关键词匹配与向量语义检索的融合（hybrid search）是目前最主流的做法，通过 Reciprocal Rank Fusion 或加权求和等方式将两种检索结果合并。2024 年以来，微软研究院提出的 GraphRAG 将知识图谱引入 RAG 流程，在全局性和多跳推理任务上表现出了优于纯向量检索的效果。

### 1.2.2 AI Agent 发展

AI Agent 的研究历史可以追溯到人工智能领域的诞生之初，但直到大语言模型展现出强大的推理和工具使用能力后，LLM-based Agent 才真正迎来了爆发式发展。Xi 等人发表的大规模综述论文将 LLM Agent 的通用框架总结为三大部分：大脑（即 LLM 本身）、感知（从外部环境获取多模态信息）和行动（通过工具与环境交互）。

在推理与行动的协同方面，Yao 等人提出的 ReAct 模式是一个里程碑式的工作，通过交替生成推理轨迹和行动，使大模型能够在推理过程中动态调整行动规划。在此基础上，Yao 等人进一步提出了 Tree of Thoughts（ToT）框架，通过显式地探索多条推理路径并自我评估各路径的可行性，实现了更深度的规划能力。在工具使用方面，Schick 等人提出的 Toolformer 展示了语言模型可以通过自监督学习掌握调用外部 API 的能力。Shinn 等人提出的 Reflexion 方法引入了口头强化学习机制，Agent 在执行任务后会对自身行为进行反思。

### 1.2.3 智能文档处理系统现状

在企业级文档管理领域，国际主流产品包括微软 SharePoint、Google Workspace、Notion 和 Confluence 等。微软通过 Copilot 将 GPT-4 集成到 Office 365 套件中。国内飞书文档集成了豆包大模型，钉钉文档则基于通义千问。在开源社区方面，LangChain 和 LlamaIndex 提供了构建 RAG 系统和 Agent 编排的 Python 框架，Dify 和 FastGPT 等面向 RAG 的应用平台提供了可视化的知识库管理界面。DocAI 系统正是针对这一空白，将 RAG 知识库、AI Agent 与微服务架构深度融合。

## 1.3 主要工作与贡献

本文设计并实现了 DocAI 智能文档协作平台，主要工作如下：（1）提出并实现了一套基于混合检索策略的 RAG 知识库系统，支持 5 种分段策略、混合检索和三级重排序；（2）实现了一个基于 ReAct 模式的 AI Agent 自主规划与执行引擎，支持 17 个工具的动态编排；（3）设计并实现了基于微服务架构的全栈系统，包括 5 个独立部署的微服务；（4）引入了全链路 AIOps 运维监控体系；（5）实现了基于 WebSocket 的实时协作编辑功能。

## 1.4 论文组织结构

本文共分为七章：第一章为绪论，介绍项目背景与国内外研究现状；第二章介绍相关技术；第三章进行系统需求分析；第四章阐述系统设计；第五章介绍核心实现；第六章展示实验与验证结果；第七章进行总结与展望。
