# DocAI 项目启动与关闭指南

---

## 一、启动项目

### 方式 A：Docker 一键启动（推荐）

> 前提：已安装 Docker Desktop + Maven 3.8+

```powershell
# 1. 打开 PowerShell，进入项目目录
cd F:\DocAI\DocAI-main

# 2. 一键启动（自动编译 + 构建镜像 + 启动服务）
.\start.ps1
```

**手动分步执行**（如果一键脚本报错）：

```powershell
# 1. Maven 编译
mvn clean package -DskipTests

# 2. 构建 Docker 镜像
docker compose build --parallel

# 3. 启动全部服务
docker compose up -d
```

**启动后验证**：

```powershell
# 查看所有容器状态
docker compose ps

# 查看启动日志
docker compose logs -f
```

### 方式 B：本机开发模式（不用 Docker）

> 前提：本机已安装 MySQL、Redis、RabbitMQ、MinIO、JDK 17+、Node.js 18+

#### 1. 启动中间件

确保以下服务已运行：

| 中间件 | 默认端口 | 检查命令 |
|--------|---------|---------|
| MySQL | 3306 | `netstat -ano \| findstr :3306` |
| Redis | 6379 | `netstat -ano \| findstr :6379` |
| RabbitMQ | 5672 | `netstat -ano \| findstr :5672` |
| MinIO | 9000 | `netstat -ano \| findstr :9000` |

#### 2. 启动后端（5 个终端窗口）

每个服务需要打开一个独立的 PowerShell 窗口：

```powershell
# 终端 1：用户服务
mvn spring-boot:run -f user-service/pom.xml

# 终端 2：文件服务
mvn spring-boot:run -f file-service/pom.xml

# 终端 3：AI 服务
mvn spring-boot:run -f ai-service/pom.xml "-Dspring-boot.run.profiles=local"

# 终端 4：文档服务
mvn spring-boot:run -f document-service/pom.xml "-Dspring-boot.run.profiles=local"

# 终端 5：网关服务
mvn spring-boot:run -f gateway-service/pom.xml
```

#### 3. 启动前端

```powershell
cd F:\DocAI\DocAI-main\vue-test-app
npm run dev
```

---

## 二、访问系统

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| API 网关 | http://localhost:8080 |
| MinIO 管理后台 | http://localhost:9001 |
| RabbitMQ 管理后台 | http://localhost:15672 |

**登录账号**：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| `admin` | `123456` | 管理员 |
| `user` | `123456` | 普通用户 |

---

## 三、关闭项目

### Docker 方式

```powershell
# 停止所有容器（保留数据）
docker compose down

# 停止并清除数据卷（慎用，会丢失所有文档和用户数据）
docker compose down -v
```

### 本机开发模式

```powershell
# 在每个终端窗口按 Ctrl+C 停止各个服务

# 确认所有进程已停止
tasklist | findstr java
```

---

## 四、常用管理命令

### 查看日志

```powershell
# 查看所有服务日志
docker compose logs -f

# 查看某个服务日志
docker compose logs -f ai-service

# 查看最近 N 行
docker compose logs --tail 50
```

### 重启单个服务

```powershell
docker compose restart ai-service
```

### 重新构建 + 启动

```powershell
# 修改代码后需要重新编译和构建
mvn clean package -DskipTests -f ai-service/pom.xml
docker compose build ai-service
docker compose up -d ai-service
```

### 进入容器内部

```powershell
# 进入 MySQL
docker compose exec mysql mysql -uroot -p123456 doc_ai

# 进入 MinIO
docker compose exec minio mc ls local/
```

---

## 五、常见问题

### 端口被占用

```powershell
# 查找占用端口的进程
netstat -ano | findstr :8080

# 强制停止进程
taskkill /PID <进程ID> /F
```

### Docker 镜像构建失败

```powershell
# 先执行 Maven 编译
mvn clean package -DskipTests

# 再重新构建
docker compose build --no-cache
```

### 前端连不上后端

检查 Vite 代理配置 `vue-test-app/vite.config.js` 中的 `apiTarget` 是否为 `http://localhost:8080`。
