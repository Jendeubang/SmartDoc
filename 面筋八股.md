# Java 后端与 AI Agent 面试八股

> 适用于 SmartDoc 文档智能处理平台和 JendaAgent 多智能体多模态创作平台面试复习。

## 一、Java 基础

### 1. 介绍一下 Java 中的 final 关键字

`final` 可以修饰变量、方法和类。

- 修饰变量：只能赋值一次。基本类型值不能改变，引用类型不能更换引用，但对象内容仍可能改变。
- 修饰方法：子类不能重写。
- 修饰类：不能被继承，例如 `String`。

`final` 不等于对象不可变：

```java
final List<String> list = new ArrayList<>();
list.add("A");              // 可以
// list = new ArrayList<>(); // 不可以
```

### 2. Java 中什么是值传递、什么是引用传递？

Java 只有值传递。

- 基本类型传递的是值的副本；
- 引用类型传递的是对象引用的副本。

因此，方法内部可以通过引用修改对象内容，但不能让外部引用指向另一个对象。

### 3. 基本类型在方法内修改，外部变量会不会受影响？

不会。方法接收的是外部变量值的副本，方法内部修改的是副本。

```java
void change(int num) {
    num = 10;
}
```

### 4. 接口、抽象类、普通父类的区别

- 接口：强调能力和规范，一个类可以实现多个接口。
- 抽象类：可以包含公共属性、普通方法和抽象方法，适合具有共同特征的对象。
- 普通父类：可以直接实例化，并提供完整实现。

接口更偏向“能做什么”，抽象类更偏向“是什么”。

### 5. 使用接口的好处，结合多态和设计模式说明

接口可以降低耦合、方便替换实现、扩展功能和单元测试。

例如项目中可以定义：

```java
interface EmbeddingProvider {
    float[] embed(String text);
}
```

后续可以替换不同的 Embedding 服务，而不需要修改 RAG 业务代码。这体现了面向接口编程、依赖倒置、多态和策略模式。

### 6. 什么是观察者模式？

观察者模式表示一对多的依赖关系。当被观察对象发生变化时，自动通知所有观察者。

项目中可以类比为：

```text
AI 任务状态变化
    ↓
WebSocket/SSE 推送模块收到通知
    ↓
前端实时更新任务进度
```

### 7. 什么是模板方法模式？

模板方法模式是在父类中定义固定流程，把部分步骤交给子类实现。

例如文档处理流程：

```text
读取文件 → 提取文本 → 清洗 → 分段 → 保存
```

不同文档类型只需要实现自己的文本提取步骤，整体流程保持不变。

### 8. HashMap 的 put 流程

以 JDK 1.8 为例：

1. 对 key 的 hash 值进行扰动处理；
2. 使用 `(n - 1) & hash` 计算数组下标；
3. 如果桶为空，直接插入；
4. 如果发生冲突，则判断 key 是否相同；
5. key 相同则覆盖 value，否则遍历链表或红黑树；
6. 链表长度达到条件后可能树化；
7. 元素数量超过扩容阈值后扩容。

默认负载因子是 `0.75`。链表长度达到 8 且数组容量至少为 64 时，会考虑转成红黑树。

### 9. JDK 1.7 和 JDK 1.8 HashMap 的区别

| 对比项 | JDK 1.7 | JDK 1.8 |
|---|---|---|
| 数据结构 | 数组 + 链表 | 数组 + 链表 + 红黑树 |
| 插入方式 | 头插法 | 尾插法 |
| 冲突严重时 | 链表查询慢 | 红黑树查询更快 |
| 扩容 | 重新计算位置 | 根据高位判断位置 |
| 线程安全 | 不安全 | 仍然不安全 |

### 10. Java 创建和启动线程的方式

1. 继承 `Thread`；
2. 实现 `Runnable`；
3. 实现 `Callable`，配合 `FutureTask`；
4. 使用 `ExecutorService` 线程池；
5. 使用 `CompletableFuture` 异步执行。

实际项目更推荐线程池，避免频繁创建和销毁线程。

### 11. 直接调用 Runnable 的 run() 方法会启动新线程吗？

不会。直接调用 `run()` 只是普通方法调用，会在当前线程执行。

只有调用 `start()` 才会创建并启动新线程。

### 12. Object 类中 wait()、notify() 的作用

- `wait()`：让当前线程等待，并释放对象监视器锁；
- `notify()`：唤醒一个等待线程；
- `notifyAll()`：唤醒所有等待线程。

调用它们必须先获得对象锁，通常在 `synchronized` 代码块中使用。`notify()` 不会立即释放锁，只有退出同步代码块后，其他线程才有机会获取锁。

### 13. 线程池的核心参数

`ThreadPoolExecutor` 主要参数：

```text
corePoolSize：核心线程数
maximumPoolSize：最大线程数
keepAliveTime：非核心线程空闲存活时间
unit：时间单位
workQueue：任务队列
threadFactory：线程创建工厂
RejectedExecutionHandler：拒绝策略
```

常见拒绝策略：

- `AbortPolicy`：直接抛异常；
- `CallerRunsPolicy`：由调用者线程执行；
- `DiscardPolicy`：直接丢弃；
- `DiscardOldestPolicy`：丢弃队列中最旧任务。

### 14. Java 类加载全过程

```text
加载 → 连接 → 初始化
```

连接阶段包括：

```text
验证 → 准备 → 解析
```

- 加载：读取字节码并生成 `Class` 对象；
- 验证：检查字节码是否合法；
- 准备：为静态变量分配内存并设置默认值；
- 解析：将符号引用转换为直接引用；
- 初始化：执行静态代码块和静态变量赋值。

类加载器通常遵循双亲委派模型。

### 15. 传输层协议有哪些？

- TCP：可靠、有连接、保证顺序；
- UDP：无连接、速度快、不保证可靠性；
- SCTP：支持多流和多宿主；
- QUIC：基于 UDP 实现的现代可靠传输协议。

HTTP、HTTPS 属于应用层协议，不是传输层协议。

## 二、Spring、认证与项目安全

### 16. Spring 中 Bean 为什么默认使用单例模式？

主要原因：

- 减少对象创建和销毁开销；
- 降低内存占用；
- 方便容器统一管理；
- 适合无状态的 Service、Controller 和配置类。

Spring 单例不代表线程安全。如果 Bean 中保存可变成员变量，仍然可能产生并发问题。

### 17. 项目中如何判断用户是否登录？

项目使用 JWT 认证：

```text
前端携带 Authorization: Bearer Token
        ↓
网关或服务解析 JWT
        ↓
验证签名、有效期和用户信息
        ↓
校验通过后放行
```

Refresh Token 保存在 Redis 中，用于续期和注销控制。

### 18. 项目中如何强制用户下线？

普通退出登录时删除 Redis 中的 Refresh Token。

管理员强制下线时，可以：

- 撤销该用户的 Refresh Token；
- 增加用户 Token 版本号；
- 将 Token 的 `jti` 加入黑名单；
- 后续请求拒绝旧 Token。

如果只删除 Refresh Token，已经签发的 Access Token 可能在过期前仍然有效。要立即失效，需要 Token 黑名单、版本号或较短的 Access Token 有效期。

## 三、集合、锁和并发

### 19. Java 常见集合及区别

- `List`：有序，允许重复；
- `Set`：不允许重复；
- `Map`：键值对结构。

常见实现：

- `ArrayList`：查询快，尾部添加快；
- `LinkedList`：适合频繁头尾操作；
- `HashSet`：无序，查询效率较高；
- `TreeSet`：自动排序；
- `HashMap`：键值对存储；
- `ConcurrentHashMap`：并发安全的 Map；
- `LinkedHashMap`：保持插入顺序。

### 20. Java 中有什么锁？

- `synchronized`；
- `ReentrantLock`；
- `ReentrantReadWriteLock`；
- `StampedLock`；
- 自旋锁；
- 悲观锁；
- 乐观锁；
- 数据库行锁；
- Redis 分布式锁。

项目中可以结合使用：

```text
Java 锁：保护单机内存任务
Redis 分布式锁：保护多实例下的索引任务
数据库锁：保护关键数据更新
```

### 21. 不可重入锁是什么？

不可重入锁是指同一个线程获得锁后，再次尝试获取同一把锁时会被阻塞。

```text
方法 A 获取锁
  ↓
方法 A 调用方法 B
  ↓
方法 B 再次获取同一把锁
  ↓
线程自己等待自己释放锁
```

容易造成死锁。`synchronized` 和 `ReentrantLock` 都是可重入锁。

## 四、缓存、数据库和消息队列

### 22. 缓存穿透是什么？项目如何解决？

缓存穿透是查询大量不存在的数据：

```text
请求 → Redis 没有 → MySQL 也没有
```

大量请求会直接打到数据库。

解决方式：

- 缓存空值；
- 使用布隆过滤器；
- 对参数进行合法性校验；
- 限制查询频率；
- 对异常 IP 和用户限流。

SmartDoc 的文档查询可以先校验文档 ID，查询不到时缓存空结果并设置较短过期时间，同时通过网关限流防止恶意请求。

### 23. 简单介绍 RabbitMQ，有什么用？

RabbitMQ 是基于 AMQP 协议的消息中间件，主要用于异步通信和服务解耦。

SmartDoc 中可以用于：

```text
文档上传
  ↓
发送解析消息
  ↓
文档服务异步解析
  ↓
发送 AI 或索引任务
```

优点：

- 降低接口响应时间；
- 服务解耦；
- 削峰填谷；
- 失败重试；
- 异步处理耗时任务。

### 24. SQL 优化有哪些方法？

- 对高频查询字段建立索引；
- 使用 `EXPLAIN` 分析执行计划；
- 避免 `select *`；
- 避免对索引字段进行函数操作；
- 避免大分页，使用游标分页；
- 减少不必要的 JOIN；
- 控制事务范围；
- 使用批量操作；
- 读多写少的数据使用缓存。

项目中文档表可以重点为以下字段建立索引：

```text
user_id
organization_id
category_id
status
created_at
```

并结合用户权限条件查询，避免全表扫描和越权查询。

### 25. 如何保证消息的准确性？

生产者：

- 开启消息持久化；
- 使用 Publisher Confirm；
- 发送失败重试；
- 使用 Outbox 事务消息。

RabbitMQ：

- 使用持久化队列；
- 生产环境使用 Quorum Queue；
- 配置死信队列。

消费者：

- 使用手动 ACK；
- 处理成功后再确认；
- 失败进入重试队列；
- 多次失败进入死信队列；
- 使用任务 ID 或幂等键防止重复执行。

文档索引任务可以通过 `documentId + contentHash + taskId` 实现幂等，避免同一文档重复索引。

## 五、项目设计模式与难点

### 26. 项目中使用了哪些设计模式？

项目中主要使用了策略模式、工厂模式、注册表模式、模板方法思想和观察者思想。

- 策略模式：文档分段策略、Rerank 策略；
- 工厂模式：`AIServiceFactory` 根据模型或任务类型创建 AI 服务；
- 注册表模式：`AgentToolRegistry` 统一注册和查找 Agent 工具；
- Provider 抽象：支持不同 Embedding、模型和文件处理实现；
- 观察者思想：任务状态变化后，通过 WebSocket 或 SSE 通知前端；
- 模板方法思想：不同文档类型复用统一处理流程。

### 27. 项目中遇到的难点是什么？

可以这样回答：

> 项目中比较有难度的是 Agent 执行链路和 RAG 知识库的稳定性。
>
> Agent 不是简单调用一次模型，而是需要经历任务规划、工具调用、结果观察和最终总结。如果工具调用失败，还要进行重试或重新规划。我通过统一执行服务管理任务状态，并增加参数校验、最大执行步数、超时、幂等键和失败重试，避免 Agent 无限循环或重复执行。
>
> RAG 方面，难点是检索准确率和权限隔离。我将文档进行分段，结合向量检索、BM25 和 Rerank 提高召回质量，同时在查询时增加用户 ID、企业 ID 和知识库 ID 条件，避免用户访问其他企业的数据。
>
> 长耗时任务通过 RabbitMQ 异步处理，Redis 保存任务状态，前端通过 SSE 或 WebSocket 获取实时进度。

### 28. 线程池在项目中使用在哪里？

项目中线程池主要用于：

- 文档解析；
- 批量向量化；
- 知识库索引；
- AI 异步任务；
- 文件处理。

例如用户上传文档后，接口不直接同步等待完整解析，而是创建任务并交给异步线程或 RabbitMQ 消费者处理，避免请求线程长时间阻塞。

线程池参数要结合任务类型配置：CPU 密集型任务线程数接近 CPU 核心数，IO 密集型任务可以适当增加，但还要考虑数据库连接池、外部模型接口和消息消费能力，不能盲目增加线程数。

## 六、项目整体总结

> 我的项目经历主要围绕 Java 微服务和 AI 应用开发展开。底层使用 Spring Boot、Spring Cloud、MySQL、Redis、RabbitMQ 和 Docker，上层结合 Spring AI、RAG 和 Agent 完成文档分析、多智能体协作和多模态创作。
>
> 我比较重视的不只是功能实现，还包括权限隔离、异步解耦、任务幂等、失败重试、限流、超时和系统可观测性。遇到问题时，我通常会从业务流程、数据一致性、并发安全和故障降级几个角度进行分析。

