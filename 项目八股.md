# SmartDoc 项目八股

> 本文用于 SmartDoc 文档智能处理平台项目面试准备。回答应以实际代码、测试结果和部署环境为准，先给结论，再补充实现细节。

## 一、项目概览

### 1. 请介绍一下 SmartDoc 项目

SmartDoc 是一个面向中小企业内部文档管理与智能处理的微服务平台，覆盖用户、文件、文档、AI 服务和企业协作等能力。AI 服务基于 Spring AI 构建，支持统一 Agent 执行链路、RAG 知识库问答、文档分析和生产力工具箱。系统通过 MySQL 保存业务数据，Redis 支持缓存、任务状态和向量数据，RabbitMQ 处理异步任务，MinIO 保存文件，Docker 完成容器化部署。

项目的核心价值是把“文档存储、文档理解、AI 操作、企业协作”整合到一条完整链路中。

### 2. 你在项目中主要负责什么

我主要负责 AI Agent、RAG 知识库、文档智能工具箱和企业协作相关能力，同时参与前端工作台、编辑器、企业空间和安全部署建设，负责从接口设计、核心实现到测试验证的完整链路。

### 3. 为什么采用微服务架构

用户、文件、文档和 AI 服务的业务边界比较清晰，发布节奏和资源需求也不同。AI 推理和索引任务比较耗时，需要独立扩缩容；文件服务需要独立处理 MinIO 和恶意文件扫描；用户和权限服务则更关注认证与数据安全。因此拆分后可以实现故障隔离、独立部署和按服务扩容。

同步调用主要用于服务间查询，耗时任务通过 RabbitMQ 异步解耦，AI 任务进度通过 Redis 持久化并使用 WebSocket 推送到前端。

## 二、AI Agent 与工具框架

### 4. Agent 的完整执行流程是什么

请求进入后，Agent 先根据用户目标生成执行计划，然后按步骤调用工具并记录工具结果；每一步执行后进行结果观察和反思，判断任务是否完成、是否缺少信息以及是否需要调整计划。如果任务没有完成，就进行重规划并继续执行，最终生成自然语言回答。

执行过程会记录 traceId、计划、工具调用、工具结果、反思和最终结果，并通过 Redis 保存任务状态、WebSocket 推送实时进度。

### 5. 对话式 Agent 和 Plan-Execute Agent 如何兼容

对话式 Agent 更接近边思考边调用工具，Plan-Execute Agent 则是先生成完整计划再执行。项目把两者统一到同一个执行引擎中：计划生成、工具执行、结果观察和反思重规划都是公共步骤，再根据场景决定是逐步决策还是按计划执行，避免重复实现两套链路。

### 6. 如何防止 Agent 无限循环

主要有三层控制：

1. 设置最大执行轮数，达到上限立即结束；
2. 反思结果判断任务已完成或不需要继续时提前退出；
3. 重规划结果为空、重复执行同一步骤或工具失败超过重试次数时停止，并返回可追踪的失败原因。

### 7. AgentToolRegistry 解决了什么问题

AgentToolRegistry 统一管理 RAG、文本处理、文件管理、PPT 生成等工具。每个工具包含名称、描述、参数 Schema 和执行器，模型调用工具前统一完成参数类型、必填项和枚举值校验。

这样新增工具时只需要注册新的执行器，不需要修改 Agent 主流程。同时可以统一处理缺参追问、模板变量解析、失败重试、危险操作审批和调用审计。

### 8. 工具调用失败如何处理

先根据工具配置进行有限次数重试，并采用退避策略；如果仍然失败，则把失败结果写入任务时间线，由反思模块决定重新规划、跳过当前步骤还是结束任务。对于有副作用的工具，需要保证幂等，避免重试造成重复写入或重复生成。

危险操作执行前需要经过审批，用户批准后继续，拒绝后终止，最终把审批和执行结果记录到审计日志中。

### 9. Spring AI 在项目中的作用

Spring AI 作为统一的大模型接入层，封装聊天模型、结构化输出和模型调用过程。Agent 的规划、反思、最终回答以及摘要、纠错、改写等能力都通过统一接口调用，减少对具体模型 SDK 的耦合，方便切换 DeepSeek 或其他兼容模型。

## 三、RAG 知识库

### 10. RAG 的完整流程是什么

文档上传后先解析正文，然后按照章节、固定长度、语义或自动策略进行分段；每个分段生成 Embedding，并把向量、正文和文档元数据保存起来；用户提问时先进行向量召回和关键词召回，再通过 Rerank 精排，最后把相关片段作为上下文交给大模型生成答案。

权限感知检索会在召回结果返回前增加用户、企业和知识库权限过滤，确保模型无法使用当前用户没有权限访问的文档内容。

### 11. 为什么要同时使用向量检索和 BM25

向量检索擅长理解语义相似内容，但对专有名词、编号和精确关键词不一定稳定；BM25 对关键词、词频和稀有词更敏感。两者结合可以同时覆盖“语义相近”和“关键词精确匹配”两类问题，再通过 Rerank 对候选结果进行精排。

### 12. HNSW 在项目中做什么

HNSW 是一种近似最近邻索引结构，通过多层图快速定位相似向量。项目使用它在内存中完成高效近邻检索，同时将向量和元数据持久化，服务重启后可以重新构建索引。这个方案适合当前中等规模数据，数据量继续增长后可以迁移到专业向量数据库。

### 13. 文档分段策略如何选择

章节分段适合标题层级清晰的技术文档，能保留上下文；固定长度分段适合无明显结构的文本，通常配合重叠窗口；语义分段根据句子语义相似度寻找切分边界；自动分段则根据文档结构选择合适策略。

### 14. 权限感知 RAG 如何实现

文档服务作为权限事实源，AI 服务检索前先获取当前用户能够访问的文档集合，召回后只保留这些文档对应的片段，并继续校验 userId、knowledgeBaseId 和 organizationId。不能只通过文档 ID 判断权限，否则可能出现跨用户或跨企业数据泄露。

### 15. RAG 指标是怎么解释的

- Top-5 召回率 93%：在 1000 条业务问答样本中，Top-5 检索结果包含正确答案对应文档或片段的比例为 93%。
- P95 响应时间 420ms：95% 的检索请求响应时间不超过 420ms，关注的是长尾性能而不是平均值。
- 核心接口成功率 99.95%：在压测口径下，成功完成的核心请求占全部请求的 99.95%，包含异常降级和失败重试机制。
- 单任务平均响应耗时 1150ms：文档 AI 任务的平均完成耗时控制在 1150ms 以内，具体统计范围需要结合测试脚本和环境说明。

### 16. 检索或 Rerank 服务异常时怎么降级

Embedding 或 Rerank 服务异常时，检索链路不能直接整体失败。可以关闭精排，退回向量分数与 BM25 的融合结果；如果外部模型超时，则使用本地关键词或简化打分兜底；索引任务则保留失败状态、错误原因和重试入口。这样能保证服务可用性，同时让问题可追踪。

## 四、KnowledgeIndexAgent

### 17. KnowledgeIndexAgent 的流程是什么

它负责管理知识库索引生命周期：接收索引任务、校验文档权限、读取内容、分段、生成向量、写入向量和元数据、更新索引状态，并在完成后进行质量评估。

索引过程支持内容 Hash 去重、旧索引清理、失败重试、任务状态查询和异步执行，状态可以从待处理、解析中、向量化、已完成流转到失败。

### 18. 如何保证索引任务幂等

通过文档内容 Hash 判断内容是否发生变化，相同内容不重复生成向量；重新索引前清理旧分片；任务状态持久化并校验当前状态，重复提交时避免重复执行。失败任务只重试失败步骤或重新进入可重试状态，避免产生脏索引。

## 五、SmartDoc AI 与工具箱

### 19. SmartDoc AI 支持哪些能力

支持指定文档问答、基于知识库总结、摘要、纠错、关键词提取、AI 改写和多语言翻译。用户可以选择一个或多个文档作为回答范围；没有选择文档时，根据用户问题检索其有权限访问的知识库内容。

对话和消息通过 MySQL 持久化，因此刷新页面或重新登录后仍然可以恢复历史记录。

### 20. 文档生产力工具箱实现了哪些功能

工具箱接入了图片 OCR、PDF 拆分合并、Word/PDF 格式转换、Word 表格导出 Excel、图片压缩裁剪旋转、图片转 PDF、图片合并成长图，以及录音 ASR 转会议纪要等能力。OCR 使用 Tesseract，语音识别使用 DashScope ASR，会议纪要由 DeepSeek 根据转写文本进行摘要和结构化整理。

### 21. OCR 或 ASR 服务失败怎么办

前端显示明确的任务状态和错误信息，后端记录任务失败原因；文件下载、MinIO 权限、模型调用和解析错误分别处理，避免全部统一成“服务器内部错误”。耗时任务使用异步队列，任务状态写入 Redis 或数据库，失败后支持重新处理。

## 六、企业空间与协同

### 22. 企业空间有哪些能力

支持企业成员、角色、部门、文档共享、评论、建议模式、版本差异、段落锁和安全分享链接。企业管理员可以管理成员和权限，普通成员只能访问授权文档；文档分类、知识库、文件和 AI 会话都需要带企业维度。

### 23. 段落锁如何避免多人同时修改同一段

用户开始编辑段落时申请锁，服务端记录段落、文档、用户和过期时间；其他用户编辑同一段时提示已被占用，不能直接覆盖。锁需要设置超时、主动释放和断线清理，避免用户关闭页面后产生永久锁。

### 24. 评论、建议模式和版本差异有什么作用

评论适合讨论具体内容，建议模式把修改记录为可接受或拒绝的提议，版本差异用于对比两次保存之间的新增、删除和修改内容。三者结合可以保留修改过程，降低多人协作时误删和误改的风险。

## 七、安全与部署

### 25. JWT 安全做了哪些改进

JWT 密钥不再硬编码，通过环境变量或 Docker Secret 注入，并支持密钥轮换：新密钥负责签发，旧密钥在过渡期继续验签，过期后再淘汰。访问令牌设置较短有效期，Refresh Token 存入 Redis，注销或管理员强制下线时删除对应会话。

另外增加登录失败锁定、密码复杂度校验、忘记密码流程，并在网关删除客户端伪造的 X-User-Id、X-Role 等请求头后重新注入可信身份信息。

### 26. 为什么微服务之间还需要内部服务令牌

不能只依赖网络隔离或客户端传来的身份请求头。内部服务接口增加服务间令牌校验，只有可信服务才能调用；网关和业务服务分别校验身份，防止绕过网关直接访问服务端口。

### 27. 多租户数据隔离怎么做

企业、部门、用户和权限信息进入查询条件，文档、文件、分类、回收站、版本、RAG 向量、索引任务、AI 会话、工具箱任务、练题集、通知、审计日志和分享链接都带 organizationId 或相关租户维度。

Redis 缓存键和 RabbitMQ 消息也要带企业或用户标识。所有查询不能只根据 documentId，而要同时校验 organizationId、userId 和 permission，并通过越权测试验证用户 A 无法读取企业 B 的数据。

### 28. 文件安全如何保证

文件存储使用 MinIO，并按用户和企业进行隔离；上传时校验文件大小、扩展名和文件头，生产环境使用 ClamAV 进行恶意文件扫描。下载、预览、转换和 OCR 都要经过权限校验，不能因为拿到文件 ID 就直接访问对象存储。

### 29. 生产部署需要注意什么

生产环境只对外暴露 Nginx 或 Traefik 的 80/443 端口，MySQL、Redis、RabbitMQ、MinIO 和微服务端口只在内部网络可访问；配置 HTTPS、生产域名 CORS、容器资源限制和非 root 用户运行。数据库需要迁移、备份和恢复演练，服务需要健康检查、日志、监控和告警。

## 八、综合追问

### 30. 项目中最难的问题是什么

最难的是把 AI 模型的不确定性和企业文档系统的确定性结合起来。模型可能返回错误参数、调用超时或生成不完整答案，因此需要 Agent 状态机、工具 Schema 校验、重试、反思重规划和审计；同时 RAG 还必须在召回前后进行权限过滤，保证“回答准确”和“数据不越权”同时成立。

### 31. RabbitMQ、Redis、MySQL 和 MinIO 分别承担什么职责

- MySQL：用户、文档、权限、会话、消息、任务等结构化业务数据。
- Redis：缓存、Refresh Token、Agent 任务快照、WebSocket 进度、向量和元数据。
- RabbitMQ：文档上传、解析、索引、AI 处理等耗时任务的异步解耦。
- MinIO：原始文件、处理结果和预览资源的对象存储。

### 32. 如果继续优化，你会做什么

第一，使用专业向量数据库或 OpenSearch 承载更大规模的索引；第二，引入更完善的评测集，分别评估召回率、答案正确性、引用准确性和幻觉率；第三，加强模型调用成本、限流、配额和敏感内容审计；第四，完善协同编辑的增量同步和冲突合并；第五，补齐生产环境的灰度发布、链路追踪和自动化灾备演练。

## 九、面试中的关键口径

1. 93%、420ms、99.95% 和 1150ms 必须说明样本、环境、统计范围和测试方式，不能只背数字。
2. HNSW 是当前中等规模场景下的轻量方案，数据量扩大后可以迁移专业向量数据库。
3. 外部模型、Rerank、ASR 和 OCR 都存在失败可能，重点说明超时、重试、降级和任务恢复。
4. 不能把“网关注入请求头”说成绝对可信，业务服务仍要校验 JWT、租户和权限。
5. 只讲自己实际负责和能够落到代码的内容，面试官追问时优先给出类名、接口、状态机或测试用例。

## 十、可以反问面试官

- 团队目前主要使用哪类 Agent 编排方式，是否已经引入 MCP 或多 Agent 协作？
- 知识库的文档规模、更新频率和主要文档类型是什么？
- 团队更关注模型效果、响应延迟还是调用成本？目前采用什么评测体系？
- 企业内部文档场景中，权限、审计和数据隔离有哪些既定规范？
