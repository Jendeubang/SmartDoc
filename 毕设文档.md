# DocAI — 基于 RAG 与 AI Agent 的智能文档协作平台

> **毕业设计**
>
> 技术栈：Spring Boot 3.2 + Spring AI + Vue 3 + Docker

---

## 一、项目简介

DocAI 是一个基于微服务架构的智能文档处理与实时协作平台。系统以 **RAG（检索增强生成）** 和 **AI Agent（自主规划-执行）** 为核心技术亮点，实现了从文档上传、AI 分析到知识库问答的完整链路。

### 核心能力一览

| 功能模块 | 技术实现 | 一句话描述 |
|---------|----------|-----------|
| 用户认证 | JWT + BCrypt | 登录/注册/角色权限 |
| 文档管理 | MinIO + MySQL | 上传/编辑/版本控制/搜索 |
| AI 摘要与关键词 | Spring AI + DeepSeek API | 自动生成文档摘要和关键词标签 |
| AI 纠错与润色 | Agent 工具编排 | 智能纠错/改写，支持写入文档或仅对话 |
| **RAG 知识库** | 向量检索 + BM25 + 重排序 | 上传文档后 AI 可基于全文知识库问答 |
| **AI Agent** | ReAct 规划-执行-反思 | 自动拆解任务、调用工具、合成回答 |
| 多模型切换 | OpenAI 兼容接口 | DeepSeek Chat / 智谱GLM-5 / 月之暗面K2.5 |
| HTML PPT 生成 | AI 生成 + 8 种主题 | 根据大纲一键生成 PPT 演示文稿 |
| 实时协作编辑 | STOMP + SockJS WebSocket | 多人同时编辑、光标同步、OT 冲突处理 |
| 文档分享授权 | 链接分享 + 角色权限 | 授权协作者（可编辑/只读）|
| AI Ops 运维 | 滑动窗口 + 故障检测 | 请求埋点、P95/P99 统计、故障告警 |
| 容器化部署 | Docker Compose | 一键启动全部 9 个服务 |

---

## 二、系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Vue 3 前端 (5173)                     │
│  工作台 │ 文档编辑器 │ Agent 工作台 │ AIOps 运维        │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP / WebSocket
                       ▼
┌───────── Gateway (Spring Cloud Gateway :8080) ──────────┐
│  路由转发 │ JWT 鉴权 │ Redis 令牌桶限流                  │
└───┬─────────┬──────────┬──────────┬─────────────────────┘
    │         │          │          │
    ▼         ▼          ▼          ▼
┌──────┐ ┌──────┐ ┌────────┐ ┌────────────┐
│ User │ │ File │ │  AI    │ │ Document   │
│ Srv  │ │ Srv  │ │  Srv   │ │  Srv       │
│:8081 │ │:8082 │ │  :8083 │ │  :8084     │
└──┬───┘ └──┬───┘ └───┬────┘ └─────┬──────┘
   │        │         │            │
   ▼        ▼         ▼            ▼
 MySQL    MinIO     Redis       RabbitMQ
                    (向量搜索)
```

### 2.2 微服务模块

| 服务 | 端口 | 职责 |
|------|------|------|
| **gateway-service** | 8080 | API 网关：路由转发、JWT 鉴权、限流 |
| **user-service** | 8081 | 用户认证：登录/注册/JWT 签发 |
| **file-service** | 8082 | 文件管理：上传到 MinIO、DOCX 解析 |
| **ai-service** | 8083 | AI 核心：RAG 检索、Agent 编排、AIOps 监控 |
| **document-service** | 8084 | 文档业务：CRUD、版本控制、协作权限 |

### 2.3 AI Agent 核心链路

```
用户输入
  │
  ▼
Agent 规划器 (LLM Planner)
  ├─→ 拆解任务步骤
  ├─→ 选择工具（共 17 个）
  │    ├─→ text-summarize   → 文档摘要
  │    ├─→ text-correct     → 纠错/润色
  │    ├─→ rag-answer       → 知识库问答
  │    ├─→ rag-search       → 知识库检索
  │    ├─→ document-write   → 写回文档
  │    ├─→ html-ppt-generate→ 生成 PPT
  │    ├─→ file-*           → 文件管理
  │    └─→ direct-answer    → 直接回答
  │
  ├─→ 反思 (Reflection)
  │    ├─→ 判断是否完成
  │    └─→ 是否需要重规划或追问
  │
  └─→ 合成最终回答
       → 存入对话记忆 (Redis)
       → 返回前端
```

### 2.4 RAG 检索链路

```
上传文档 → 分段引擎 (5 种策略)
  ├─ AUTO      自动检测章节/长度
  ├─ CHAPTER   按章节切分
  ├─ FIXED     固定长度切分  
  ├─ SEMANTIC  语义相似度切分
  └─ HYBRID    章节 + 语义混合

  ↓ 向量化 (Embedding API)

  ↓ 存入 Redis VectorStore

用户提问 → 混合检索
  ├─ 向量检索（语义相似度）
  ├─ BM25 关键词检索
  ├─ 同义词扩展
  └─ 上下文贯通 (bridgeContext)

  ↓ 重排序 (Reranker)
  ├─ BM25 分数融合
  ├─ Cross-Encoder API
  └─ HYBRID 混合

  ↓ 大模型生成回答 → 返回前端
```

---

## 三、技术栈详情

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 21 | 开发语言 |
| Spring Boot | 3.2.0 | 微服务框架 |
| Spring Cloud | 2023.0.3 | 服务治理（Gateway + Feign） |
| Spring AI | 1.0.0-M4 | AI 模型集成 |
| Spring Security | 6.2.0 | JWT 认证 |
| MyBatis-Plus | 3.5.5 | ORM 框架 |
| MySQL | 8.0 | 业务数据库 |
| Redis | 7.0 | 缓存 + 向量搜索 + 会话管理 |
| RabbitMQ | 3.8.8 | 异步任务消息队列 |
| MinIO | latest | 对象存储（文档文件） |
| JJWT | 0.11.5 | JWT 令牌 |

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.5.25 | 前端框架 |
| Vite | 4.5.3 | 构建工具 |
| Element Plus | 2.14.0 | UI 组件库 |
| Axios | 1.13.5 | HTTP 请求 |
| STOMP.js + SockJS | - | WebSocket 实时通信 |

### 部署

| 技术 | 用途 |
|------|------|
| Docker | 容器化 |
| Docker Compose | 一键编排 9 个容器 |
| eclipse-temurin:17-jdk | 基础镜像 |

---

## 四、核心功能与 API

### 4.1 AI 同步接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/ai/models` | 获取可用模型列表 |
| POST | `/api/ai/summarize` | 文档摘要 |
| POST | `/api/ai/keywords` | 关键词提取 |
| POST | `/api/ai/analyze` | 文本统计分析 |
| POST | `/api/ai/correct` | 文档纠错 |

### 4.2 AI 异步接口（RabbitMQ）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai/async/summarize` | 异步摘要 |
| POST | `/api/ai/async/keywords` | 异步关键词 |
| POST | `/api/ai/async/agent/execute` | 异步 Agent 任务 |
| GET | `/api/ai/async/jobs/{jobId}` | 轮询任务结果 |

### 4.3 Agent 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai/agent/execute` | 执行 Agent 任务（同步） |
| GET | `/api/ai/agent/tools` | 获取工具列表 |
| POST | `/api/ai/agent/approvals/confirm` | 确认危险操作 |
| GET | `/api/ai/agent/conversations/{id}/history` | 获取对话历史 |

### 4.4 RAG 知识库接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai/rag/index` | 索引文档 |
| POST | `/api/ai/rag/index/segment` | 指定策略索引 |
| GET | `/api/ai/rag/search` | 向量检索 |
| GET | `/api/ai/rag/search/hybrid` | 混合检索 |
| POST | `/api/ai/rag/query` | RAG 问答 |

### 4.5 文档管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/documents/user/{userId}` | 文档列表 |
| GET | `/api/documents/{id}` | 文档详情 |
| POST | `/api/documents` | 创建文档 |
| PUT | `/api/documents/{id}` | 更新文档 |
| DELETE | `/api/documents/{id}` | 删除文档 |
| GET | `/api/documents/{id}/versions` | 版本列表 |
| POST | `/api/documents/{id}/restore/{v}` | 恢复版本 |
| POST | `/api/documents/{id}/collaborators/{u}` | 授权协作者 |
| DELETE | `/api/documents/{id}/collaborators/{u}` | 取消授权 |

### 4.6 AIOps 监控接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/ai/aiops/health` | 健康检查 |
| GET | `/api/ai/aiops/monitor` | 监控指标（1m/5m/15m 窗口） |
| GET | `/api/ai/aiops/detect` | 触发故障检测 |
| GET | `/api/ai/aiops/faults` | 故障列表 |
| POST | `/api/ai/aiops/faults/{id}/resolve` | 处理故障 |

### 4.7 PPT 生成

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/skills/html-ppt/generate` | 生成 PPT（返回 JSON） |
| POST | `/api/skills/html-ppt/view` | 生成并预览 |
| POST | `/api/skills/html-ppt/download` | 生成并下载 |

### 4.8 用户认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/users/login` | 登录 |
| POST | `/api/users/register` | 注册 |
| GET | `/api/users/{id}` | 用户信息 |
| GET | `/api/users/lookup?username=` | 按用户名查找 |

---

## 五、数据库设计

数据库 `doc_ai` 包含以下核心表：

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `user` | 用户 | id, username, password(BCrypt), email, role, status |
| `file_metadata` | 文件元数据 | id, file_name, file_size, bucket_name, object_key |
| `document` | 文档 | id, title, content, summary, user_id, file_id, version |
| `document_access` | 协作权限 | document_id, user_id, role(owner/editor/viewer) |
| `document_version` | 版本历史 | document_id, version_number, content, change_log |
| `mcp_audit_log` | AIOps 审计 | skill_name, params, status, user_id |

---

## 六、部署与启动

### 环境要求

- Docker Desktop 4.0+
- Maven 3.8+
- Node.js 18+

### 一键启动

```powershell
.\start.ps1
```

脚本自动执行：Maven 编译 → Docker 构建 → 启动 9 个容器 → 等待就绪

### 服务地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| API 网关 | http://localhost:8080 |
| MinIO 管理 | http://localhost:9001 |
| RabbitMQ 管理 | http://localhost:15672 |
| 测试账号 | admin / 123456 |

### Docker 容器一览

```
mysql:8.0      → 数据库
redis:7.0      → 缓存 + 向量搜索
rabbitmq:3.8   → 消息队列
minio/minio    → 对象存储
user-service   → 用户服务
file-service   → 文件服务
ai-service     → AI 服务
document-service → 文档服务
gateway-service  → API 网关
```

---

## 七、项目亮点（答辩可用）

### 技术亮点

1. **RAG 混合检索**：向量检索 + BM25 关键词 + 重排序三级融合，5 种分段策略对比实验
2. **AI Agent 自主规划**：ReAct 循环 + 反思机制 + 17 个工具编排 + 危险操作审批
3. **实时协作编辑**：STOMP + SockJS WebSocket + 基础 OT 冲突处理
4. **全链路可观测**：AIOps 自动埋点 + P95/P99 统计 + 故障自动检测
5. **多模型抽象**：Spring AI `ChatModel` 统一接口，支持 4 种大模型一键切换

### 工程亮点

- **5 个微服务独立部署**，网关统一入口
- **Docker Compose 一键部署**，9 个容器编排
- **RabbitMQ 异步解耦**，AI 任务不阻塞主流程
- **MinIO + MySQL 分层存储**，文件与元数据分离
- **前端 Vue 3 + Element Plus**，6 个路由页面

---

## 八、后续可扩展方向

- 接入 Nacos 实现服务注册发现
- 使用 Elasticsearch 替代 Redis 向量搜索（生产级）
- 对接飞书/钉钉 Webhook 告警
- 增加单元测试覆盖率（当前约 15%）
- 补充 JMeter 压力测试报告
